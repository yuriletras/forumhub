# Configura��o do banco de dados MySQL
# A primeira linha � o endere�o do seu banco de dados
spring.datasource.url=jdbc:mysql://localhost:3306/forumhub_db
# Estas duas linhas s�o o login e a senha para se conectar ao banco de dados
spring.datasource.username=root
spring.datasource.password=Yan0685@@@!
# Aqui, avisamos ao Spring que estamos usando o MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Configura��es do JPA (que o Spring usa para interagir com o banco)
# `update` faz com que o Spring crie e atualize as tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
# `true` faz com que o Spring mostre os comandos SQL no console. � �timo para o desenvolvimento
spring.jpa.show-sql=true
